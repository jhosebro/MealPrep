import { View } from 'react-native';

interface SidebarNavigationProps {
  activeRoute: string;
}

export function SidebarNavigation({ activeRoute: _activeRoute }: SidebarNavigationProps) {
  return <View />;
}
